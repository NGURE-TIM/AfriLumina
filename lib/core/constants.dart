
import 'package:flutter/material.dart';


///Base API Endpoints used for fetching data.
const apiUrl =
    "https://restcountries.com/v3.1/"; 

/// Google Maps search URL template for opening location coordinates.
const mapUrl = "https://www.google.com/maps/search/?api=1&query=";

/// Defines the primary colors used throughout the application.
class AppColors {
  static const MaterialColor baseColor = Colors.deepOrange;
  static const MaterialColor highlightColor = Colors.orange;
  static const MaterialColor errorColor = Colors.red;
}